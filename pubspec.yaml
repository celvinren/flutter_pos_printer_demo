name: flutter_pos_printer_demo
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  flutter_esc_pos_utils: ^1.0.1
  # flutter_thermal_printer: ^0.0.16
  image: ^4.5.2
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  thermal_printer: ^1.0.5
  tray_manager: ^0.3.1
  window_manager: ^0.4.3

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
